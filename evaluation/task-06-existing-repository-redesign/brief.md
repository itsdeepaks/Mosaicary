# Task 06 — Existing-repository redesign

The current repository contains a static design-resource library with search, category/access filters, cards, and a Markdown tab. Plan a targeted mobile usability redesign for the resource-browsing flow. Preserve its resource data and visual language, reuse the existing toolbar/card/tab primitives, and avoid adding any dependency or new product area.

The existing mobile toolbar currently overflows at a 390px viewport. The plan must resolve the browsing flow without turning the library into the product's primary dashboard.

Plan first. Do not implement in the plan phase.
