id: task-06-existing-repository-redesign
category: existing-repository-redesign
repository_binding: current-repository-at-frozen-commit
target: resource-library-mobile-browsing
required_states: [library-default, filtered-results, no-results, markdown-tab, csv-load-error]
viewports: [390x844, 768x1024, 1440x1000]
focus: [component-reuse, responsive-repair, accessibility, scope-discipline]
implementation_constraints:
  - Reuse the existing CSS variables, toolbar controls, cards, tabs, and CSV/Markdown data paths.
  - Do not add a framework or component library.
  - Do not redesign the page as a new product dashboard.
  - Preserve keyboard access and the current desktop interaction model unless the contract states a reason to change it.
status: ready-for-dry-run
