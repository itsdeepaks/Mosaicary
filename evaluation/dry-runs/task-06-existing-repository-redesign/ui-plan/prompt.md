# Frozen task prompt

Plan a targeted mobile usability redesign for the existing resource-browsing flow. Preserve its resource data and visual language, reuse the existing toolbar/card/tab primitives, and avoid adding any dependency or new product area.

The existing mobile toolbar currently overflows at a 390px viewport. The plan must resolve the browsing flow without turning the library into the product's primary dashboard.

Plan first. Do not implement in the plan phase.
