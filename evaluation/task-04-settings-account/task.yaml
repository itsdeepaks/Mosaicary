id: task-04-settings-account
category: settings-account
repository_binding: pending-isolated-settings-fixture
target: workspace-settings
required_states: [default, unsaved-changes, saving, saved, permission-denied, validation-error, destructive-confirmation, delete-failed]
viewports: [390x844, 768x1024, 1440x1000]
focus: [categorization, permissions, destructive-actions, save-feedback]
implementation_constraints:
  - Keep destructive actions visually and semantically distinct.
  - Do not rely only on a color change to signal dangerous actions.
  - Make permission differences legible without exposing unauthorized controls.
status: ready-for-repository-binding
