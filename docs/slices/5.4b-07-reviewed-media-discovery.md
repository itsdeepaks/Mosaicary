# Slice 5.4b-07 — reviewed Open Graph and Twitter discovery batch

Status: **contract committed — deterministic selection and discovery pending**

## Goal

Process the next twenty `pending` resources in deterministic catalogue order.
Discover only official Open Graph, Twitter preview, and favicon metadata from
canonical websites, manually inspect every exact raster considered for
publication, and assign a truthful terminal coverage disposition to every
selected resource.

## Starting point

- base `main`: `7d3b28e8debd46a120de59ecd5b7be0a76923403`;
- approved media: **70**;
- pending: **167**;
- terminal without approved media: **58**;
- total resources: **295**.

## Acceptance criteria

- select exactly the next twenty pending stable IDs with the repository selector;
- fetch canonical official pages only;
- preserve priority and exact provenance:
  `og:image:secure_url` → `og:image` → `twitter:image` →
  `twitter:image:src` → favicon → generated mark;
- validate HTTPS/public DNS/redirect/timeout/size/MIME/credential boundaries;
- keep discovery candidates and exact raster bytes in short-lived artifacts only;
- manually inspect each raster before approval;
- reject unrelated, generic, promotional, login, cookie, ad, campaign-overlay,
  broken, unsafe, insecure, unsupported, or low-information imagery;
- bypass no authentication, anti-bot, paywall, rate-limit, redirect, or private
  network boundary;
- terminalize all twenty selected coverage records;
- update only reviewed media/coverage sources, deterministic outputs, focused
  assertions, progress ledger, and this evidence note;
- preserve all catalogue IDs/slugs/facts and card/runtime behavior;
- run focused validation, full checks, build, responsive browser/media-fallback
  checks at 1440, 1024, 768, and 390 pixels, release gate, and Vercel;
- remove every temporary workflow, script, artifact, and downloaded raster before
  final diff review;
- squash-merge before Batch 8 begins.

## Exclusions

No screenshots, third-party binary commits, unofficial sources, search-result
images, proxy/cache/wildcard optimizer, runtime crawler, dependency, catalogue
fact, UI, auth, Supabase, RLS, form, MCP, or unrelated change. No more than
twenty resources and no Batch 8 work in this branch.

## Rollback

Revert the eventual squash commit. No external binary or service state requires
cleanup.
