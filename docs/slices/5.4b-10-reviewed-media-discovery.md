# Slice 5.4b-10 — reviewed Open Graph and Twitter discovery batch

Status: **contract committed — discovery pending**

## Goal

Process the next twenty `pending` coverage records in deterministic catalogue order using canonical official metadata, exact-raster manual review, and repository-managed reviewed publication.

## Starting state

- base: `3f9579dc01799dbc44ab60c0673d1023b3aab5ae`;
- approved production media: **112**;
- pending: **107**;
- terminal without approved media: **76**;
- total catalogue resources: **295**.

## Acceptance criteria

- exactly the next deterministic set of at most twenty pending IDs is selected;
- only official canonical `og:image`, Twitter image, and raster favicon candidates are discovered;
- every exact raster response is technically and visually reviewed before production approval;
- every selected resource receives a terminal coverage disposition;
- candidate evidence remains separate from production records;
- focused and complete validation, responsive widths, image-failure fallback, security, performance, and complete-diff checks pass;
- merge completes before Batch 11 begins.

## Exclusions

No screenshots, committed third-party binaries, unofficial sources, image proxy/cache, broad remote allowlist, runtime crawler, heavy image dependency, catalogue fact changes, UI/auth/database work, or Batch 11 work.
