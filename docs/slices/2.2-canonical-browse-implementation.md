# Product Plan v2 / Slice 2.2 — Canonical Browse Implementation

Status: **DONE**  
Depends on: Phase 2 / Slice 2.1 Browse contract  
Branch: `feat/canonical-browse`

## Goal

Replace the competing Explore and Full Reference catalogue experiences with one paginated, shareable, responsive `/resources` browser that evaluates a Tessli source internally before sending a visitor to the provider.

## Acceptance criteria

- `/resources` is the only complete source browser;
- server derivation sends only the selected result page to interactive result views;
- cards render at most 24 results;
- compact list and table render at most 50 rows;
- URL state restores query, category, access, source type, profile level, sort, view, and page;
- invalid/default/legacy URL state normalizes deterministically;
- changing query/filter/sort/view resets page to one;
- browser Back and Forward restore canonical state;
- only the selected result view is rendered;
- no duplicate full mobile/desktop result map exists;
- `Recently verified` is omitted while the canonical baseline contains zero Verified sources;
- old `sort=verified` does not create a fake verification order;
- hard-coded verification dates are removed;
- cards, list, and table have distinct responsibilities;
- internal `/resources/[slug]` navigation is primary;
- Save remains independent and works in every result view;
- Visit source remains a separate safe external action;
- unavailable sources retain an internal profile and do not expose a misleading provider action;
- homepage search submits to `/resources`;
- homepage catalogue rendering is reduced to a maximum of twelve compatibility-preview results;
- legacy useful query state is preserved when moving to `/resources`;
- search, pagination, internal navigation, and external navigation remain useful without JavaScript;
- empty, pending, error, and out-of-range page states are truthful;
- focus, result count, pagination, table, filter, and view controls are accessible;
- required responsive checks pass without page-level overflow;
- focused tests, full Web CI, release gate, complete diff review, and browser evidence pass before merge.

## Implemented

- canonical parser, serializer, and deterministic result derivation in `web/lib/browse.ts`;
- server-rendered `/resources` with 24-card and 50-row pagination;
- card, compact-list, and horizontally contained table views using one selected result tree;
- internal Tessli profile links as primary actions plus independent Save and provider actions;
- static minimum `/resources/[slug]` routes for all 295 source slugs;
- homepage search routed to `/resources` and homepage catalogue bounded to twelve preview cards;
- canonical route, history, responsive, accessibility, and release-browser coverage;
- CI assertions migrated away from the superseded Full Reference contract.

## Complete-diff findings and fixes

- removed temporary inspection, updater, and formatter workflows before final validation;
- replaced stale Web CI assertions that still expected 295/48 homepage results and Full Reference markers;
- fixed mobile Browse overflow by allowing grid children, controls, text, and the table scroller to shrink or wrap correctly;
- retained the minimum source-detail boundary instead of expanding into Slice 2.3;
- preserved the 275 Listed / 20 Profiled / 0 Verified truth and omitted fake verification ordering;
- introduced no dependency, schema migration, provider call, credential, Supabase mutation, or deployment-state change.

## Validation contract

The exact final PR head must pass:

- Prettier formatting;
- TypeScript;
- ESLint;
- all repository tests and source/media validators;
- catalogue drift;
- production build, including all 295 static source-profile routes;
- Web CI canonical route/browser matrix;
- Phase 1 Release Gate browser matrix;
- complete PR diff review.

Run IDs and the squash merge commit are recorded in PR #79 after exact-head validation.

## Exclusions preserved

- no mature Source Detail content beyond the minimum non-dead boundary;
- no universal Compare;
- no semantic/vector search;
- no new profiles merely to populate filters;
- no live provider verification;
- no local Boards or research-pack export;
- no authentication, cloud, submission, or moderation work;
- no mature homepage redesign;
- no new MCP tools;
- no mass screenshot work;
- no unrelated refactor;
- no dependency addition.

## Rollback

Revert the squash commit. The repository-managed catalogue, source-profile contract, Saved data format, MCP, Supabase groundwork, and deployment history remain independently recoverable.
