# Slice 7.1e — UI Realignment: Grain, Hero & Mobile Card Frame

Status: **active implementation slice**  
Branch target: `feat/ui-realignment-grain-hero-cards`

## Goal

Calibrate page atmosphere grain opacity to match `design.md` §7 (`0.018–0.028`), collapse/reduce Explore hero artwork height on narrow mobile viewports (<=390px) per `page-contracts.md` §1, and fix 1-column mobile card border collapse to prevent double-thickness border artifacts.

## Acceptance Criteria

- [ ] `web/app/globals.css`: Page canvas noise texture opacity calibrated down to `0.022` (within `design.md` §7 contract `0.018–0.028`).
- [ ] `web/components/explore-hero/explore-hero.module.css`: Hero artwork height reduced on `<=390px` viewports (`height: 120px` / `max-height: 140px`) to bring search input and value statistics into immediate mobile view.
- [ ] `web/components/resource-card/resource-card.module.css`: Mobile 1-column card grid borders use single-line shared borders without double-thickness overlap artifacts.
- [ ] All existing unit tests and browser smoke tests pass (`npm run typecheck`, `npm run lint`, `npm test`, `npm run build`).
- [ ] `build-slices.md` records `7.1e DONE / 7.2a NEXT`.

## Exclusions

- No changes to catalogue data or JSON schemas.
- No Supabase or auth changes.
- No toast component or saved page changes (deferred to Slice 7.2a).
- No table column changes on Full Reference (deferred to Slice 7.2b).
