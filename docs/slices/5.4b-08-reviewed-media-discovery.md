# Slice 5.4b-08 — reviewed Open Graph and Twitter discovery batch

Status: **contract committed — discovery pending**

## Goal

Review the next twenty deterministic `pending` resources through official
canonical Open Graph/Twitter/favicon discovery, exact-raster visual inspection,
manual approved-media copy, and terminal coverage dispositions.

## Starting point

- base `main`: `c7b444c669992c446d408b6532e18f8ec4dc61f0`;
- approved: **86**; pending: **147**; terminal without media: **62**.

## Acceptance criteria

Select at most twenty pending IDs in catalogue order; fetch canonical official
pages only; preserve exact OG/Twitter provenance; enforce HTTPS, public-network,
redirect, timeout, size, and raster MIME limits; keep candidates and raster bytes
in short-lived artifacts; manually inspect every raster; terminalize all selected
coverage rows; regenerate deterministic outputs; pass focused/full checks,
build, responsive/fallback QA at 1440/1024/768/390, release gate, and Vercel;
remove every temporary workflow/script/artifact before squash merge.

## Exclusions

No screenshots, binaries, unofficial sources, proxy/cache/wildcard optimizer,
runtime crawler, dependency, catalogue fact, UI, auth, Supabase, database, or
Batch 9 work.

## Rollback

Revert the eventual squash commit; no external state cleanup is required.
