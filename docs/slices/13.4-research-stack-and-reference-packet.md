# Slice 13.4 — Research Stack Builder & Reference-Packet Export

Status: **ready for merge**

Branch target: `feat/research-stack-packet-export`

## Goal

Build a file-backed research stack builder and reference-packet Markdown generator (`web/lib/research-packet.ts`) that turns selected repository resources and their validated intelligence profiles into an actionable handoff packet. The packet must preserve evidence provenance and separate facts from Tessli classifications, as required by the provider-boundary contract.

## Acceptance Criteria

- [x] Create `web/lib/research-packet.ts`:
  - Provides `buildResearchStack(taskName, resourceIds)` function that compiles an ordered research plan.
  - Provides `generateMarkdownReferencePacket(stack)` function producing deterministic Markdown with resource metadata, capabilities, governance restrictions, evidence provenance, and handoff instructions.
- [x] Create `web/tests/research-packet.test.mjs` verifying research stack assembly, deterministic Markdown output, and evidence preservation.
- [x] All CI checks pass (`typecheck`, `lint`, `test`, `build`).
- [x] `build-slices.md` records `13.4 DONE / 13.5 NEXT`.

## Exclusions

- No live network scraping.
- No Supabase or auth changes.
