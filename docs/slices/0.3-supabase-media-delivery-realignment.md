# Slice 0.3 — Supabase and Media Delivery Realignment

Status: **DONE**

## Goal

Replace the stale project-creation handoff with repository truth for the
existing Tessli Supabase project, and define the bounded path from the reviewed
media pilot to complete catalogue research coverage.

## Acceptance criteria

- `CODEX-SUPABASE-HANDOFF.md` directs future work to select and verify the
  existing Tessli project rather than create a project or request cost approval;
- `build-slices.md` records revised Slice 10.1b as the next account slice and
  preserves every downstream dependency;
- the handoff forbids mutations through the unrelated connected Supabase
  account and keeps all real environment values outside Git;
- the media roadmap separates coverage architecture, reviewed discovery
  batches, and screenshot-policy work;
- media research completion requires an explicit outcome for all 295 resources
  and does not count the generated-letter rendering fallback as research;
- manual approval, network-free CI, and existing SSRF protections remain
  mandatory;
- Figma Community, UI8, and Creative Market remain pending and are not treated
  as approved or blocked without review;
- no runtime, data, schema, dependency, environment, or cloud state changes.

## Exclusions

- no Supabase, Vercel, OAuth, SMTP, or Resend mutation;
- no project creation, schema migration, RLS policy, or auth activation;
- no media discovery, candidate review, production-media copy, or screenshot;
- no MCP, application, test, workflow, dependency, or generated-data change;
- no dependency-hardening work or unrelated documentation cleanup.

## Repository evidence

- branch base: `aeda945f7c745a13783f01a3925125971aed7d7e`;
- the existing local public Supabase configuration remains ignored and its
  values are not recorded here;
- approved media remains 8 rows: 7 previews and 5 favicons;
- the candidate queue remains 8 rows: 5 approved-for-copy candidates and 3
  pending records;
- the public catalogue remains 295 repository-managed resources.

## Security and privacy

The project URL, publishable key, service-role material, database password,
access tokens, OAuth secrets, and SMTP credentials are absent from this slice.
The documentation distinguishes public client reachability from authenticated
management authority and prohibits use of the unrelated connected Supabase
account.

## Verification

The exact branch diff must remain documentation-only. Run formatting checks,
catalogue validation, media-review validation, repository tests, type checking,
lint, and production build before merge. Browser QA is not applicable because
the slice changes no rendered behavior.

## Rollback

Revert the documentation-only squash commit. No runtime, environment, database,
provider, deployment, or media state requires rollback.
