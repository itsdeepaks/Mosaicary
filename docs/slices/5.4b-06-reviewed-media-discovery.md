# Slice 5.4b-06 — reviewed Open Graph and Twitter media discovery

Status: **contract committed — selection and discovery pending**

## Goal

Select the next deterministic set of at most twenty `pending` resources from
`lib_data/resource-media-coverage.json`, discover only official Open Graph,
Twitter preview, and favicon metadata from each canonical site, manually inspect
every candidate considered for publication, and give every selected resource a
truthful terminal coverage disposition.

## Acceptance criteria

- branch starts from refreshed `main` at
  `0edaa64d4dbeed790c452b0805503f944a3b0325`;
- the repository selector chooses the first twenty pending stable resource IDs
  in canonical catalogue order;
- discovery runs only through the explicit bounded network command and writes
  temporary review artifacts outside committed production sources;
- candidate priority remains approved manual media, official `og:image`,
  official `twitter:image`/`twitter:image:src`, approved favicon, then generated
  mark;
- every approved preview is declared by the canonical source page, is HTTPS,
  credential-free public raster media, and is manually inspected before
  publication;
- redirects, HTTP failures, rate limits, authentication or anti-bot boundaries,
  unsupported types, SVG-only metadata, mismatched brands, generic imagery, and
  incomplete visual review are recorded truthfully without bypass;
- candidate evidence remains separate from `lib_data/resource-media.json` and
  cannot publish itself automatically;
- every selected resource reaches `approved-media`, `no-suitable-raster`,
  `blocked`, `failed`, or `rejected` with a checked date and evidence note where
  required;
- approved media, coverage, generated catalogue, deterministic reports, and
  focused count assertions reconcile;
- normal build, tests, catalogue generation, and CI remain network-free;
- resource cards retain fixed aspect ratio, preview → favicon → generated-mark
  fallback, lazy loading, async decoding, and no-referrer behavior;
- format, typecheck, lint, all tests, media/candidate/coverage/catalogue checks,
  production build, browser interactions, image-failure fallbacks, and no
  horizontal overflow pass at 1440, 1024, 768, and 390 pixels;
- the complete diff contains no secret, external binary, screenshot asset,
  unrestricted proxy/optimizer setting, dependency, unrelated UI/auth/Supabase
  change, or catalogue fact edit;
- the final PR is reviewed, exact-head checks pass, and the slice is
  squash-merged before Batch 7 begins.

## Exclusions

- no screenshot capture, screenshot fallback, or image binary commit;
- no Google Images, search thumbnails, Pinterest, social repost, stock image,
  third-party proxy, or unrelated marketing asset;
- no unrestricted image proxy, wildcard optimizer, object-storage cache, or
  broad remote-host allowlist;
- no runtime metadata fetching and no network request during normal build/test/CI;
- no bypass of authentication, anti-bot controls, rate limits, browser safety,
  private networks, redirect limits, response-size limits, or content-type
  validation;
- no public catalogue row, stable ID, slug, category, access, description, or
  product-fact change;
- no card layout, auth, Supabase, RLS, form, MCP, dependency, or unrelated
  documentation work;
- no seventh discovery batch in this branch.

## Expected source and generated files

The final implementation may change only the reviewed media/coverage sources,
their deterministic outputs, focused media assertions, the progress ledger, and
this slice evidence. Temporary discovery workflows and artifacts must be removed
before final diff review.

## Rollback

Revert the eventual squash commit. This must restore the selected coverage rows,
approved-media sidecar, generated catalogue/report fingerprints, and focused
assertions without requiring deletion of remote binaries or external service
state.
