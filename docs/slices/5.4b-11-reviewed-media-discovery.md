# Slice 5.4b-11 — reviewed Open Graph and Twitter discovery batch

Status: **contract committed — discovery pending**

## Goal

Process the next twenty `pending` coverage records in deterministic catalogue order using canonical official metadata, exact-raster manual review, and repository-managed reviewed publication.

## Starting state

- base: `7cbcacc97b67e9466b1dda357f50cae9cbaa7f3d`;
- approved production media: **123**;
- pending: **87**;
- terminal without approved media: **85**;
- total catalogue resources: **295**.

## Acceptance criteria

- exactly the next deterministic set of at most twenty pending IDs is selected;
- only official canonical `og:image`, Twitter image, and raster favicon candidates are discovered;
- every exact raster response is technically and visually reviewed before approval;
- every selected resource receives a terminal coverage disposition;
- candidate evidence remains separate from production records;
- focused/full validation, responsive widths, image-failure fallback, security, performance, and complete-diff checks pass;
- merge completes before Batch 12 begins.

## Exclusions

No screenshots, committed third-party binaries, unofficial sources, image proxy/cache, broad remote allowlist, runtime crawler, heavy dependency, catalogue fact changes, UI/auth/database work, or Batch 12 work.
