# Slice 9.4 — Phase 1 audit remediation

Status: **DONE — locally verified 2026-08-02**

## Goal

Resolve the verified Phase 1 visual-contract and responsive findings from the
2026-08-02 frontend audit without changing the public catalogue, media track,
or deferred authentication scope.

## Changes

- fixed page grain to the viewport as required by `design.md`;
- aligned the global four-column layout transition with the 768px mobile
  navigation breakpoint;
- moved the resource intelligence badge from inline styling into its CSS module
  and applied the approved light-surface `--accent-text` token;
- assigned the Full Reference verification-date column an explicit width and
  non-wrapping date treatment;
- adjusted desktop navigation weight from 600 to the contract-compliant 550.

## Audit findings not changed

- The skip link is already imported by the root layout and receives a visible
  keyboard-focus treatment from `site-header-global.css`.
- Full Reference already replaces the desktop grid with compact rows below
  1099px; the audit's mobile browser was not at a mobile viewport.
- `Ctrl / ⌘ K` is the approved platform-neutral shortcut label.
- The `/suggest` link deliberately leads to a truthful Phase 1 placeholder;
  live community forms remain Slice 12.1.
- Authentication, including Google, remains deferred. No sign-in behaviour,
  provider setting, credential, or email work is introduced.

## Verification

- focused static tests cover the corrected grain, breakpoint, badge, and
  table-column contracts;
- formatting, TypeScript, lint, all Node tests, and the production build pass;
- the local development server is used for browser inspection at desktop and
  mobile widths.

## Rollback

Revert this slice commit. The changes are CSS and static-contract tests only;
no data, external configuration, or database state requires cleanup.
