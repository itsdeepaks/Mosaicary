# Slice 13.3 — Resource Detail Intelligence UI & Advanced Capability Filters

Status: **active implementation slice**  
Branch target: `feat/intelligence-ui-filters`

## Goal

Expose the 20-profile resource intelligence dataset in the Tessli UI by rendering capability badges, integration indicators (MCP/CLI/API), supported frameworks/tools, and evidence status on resource components, and providing an intelligence filter toggle in `DiscoveryControls` per `UI-INTELLIGENCE-ROADMAP.md` §8.

## Acceptance Criteria

- [ ] Create `web/lib/intelligence.ts`:
  - Module that loads and maps the 20 intelligence profile JSONs from `web/data/intelligence-profiles/`.
  - Provides `getIntelligenceProfile(resourceIdOrSlug)` lookup function.
  - Exposes capability labels and integration indicator helpers.
- [ ] Update `web/components/resource-card/resource-card.tsx` & `resource-card.module.css`:
  - Render a subtle capability badge or integration indicator (e.g., `MCP` / `CLI` / `API`) when an intelligence profile exists for a resource.
- [ ] Update `web/components/explore-discovery/discovery-controls.tsx`:
  - Add an optional "Intelligence capability" filter pill group (e.g. "MCP-enabled", "Generative UI", "Section inspiration", "Component library").
- [ ] Create `web/tests/intelligence-ui.test.mjs` testing profile lookup, badge rendering, and filter filtering.
- [ ] All CI checks pass (`typecheck`, `lint`, `test`, `build`).
- [ ] `build-slices.md` records `13.3 DONE / 13.4 NEXT`.

## Exclusions

- No external network scraping or live credentials.
- No Supabase or auth changes.
