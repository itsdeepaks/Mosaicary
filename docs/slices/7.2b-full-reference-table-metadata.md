# Slice 7.2b — Full Reference Table Metadata & Verification Date Sorting

Status: **active implementation slice**  
Branch target: `feat/full-reference-table-metadata`

## Goal

Expose the "Last verified" date metadata column in the desktop Full Reference table view and connect verification date sorting per `component-contracts.md` §12.

## Acceptance Criteria

- [ ] Create `docs/slices/7.2b-full-reference-table-metadata.md`.
- [ ] Update `web/components/full-reference/full-reference-experience.tsx`:
  - Include "Last verified" column in the desktop `<table />` layout.
  - Render verified dates or formatted verification status for resources.
  - Connect sorting controls to allow sorting by verification date.
- [ ] Update `web/tests/full-reference.test.mjs` to test "Last verified" column rendering and sorting.
- [ ] All CI checks pass (`typecheck`, `lint`, `test`, `build`).
- [ ] `build-slices.md` records `7.2b DONE / 13.2 NEXT`.

## Exclusions

- No third-party network metadata fetching or scraping.
- No Supabase or auth changes.
